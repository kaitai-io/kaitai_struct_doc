dist: bionic
sudo: false

branches:
  only:
    - master

language: ruby

addons:
  apt:
    packages:
      - graphviz
      - python3-pygments

cache:
  - apt

before_install:
  - gem install asciidoctor pygments.rb
  - pygmentize -V

script:
  - pygmentize -V
  - make

after_success:
  - ./publish
