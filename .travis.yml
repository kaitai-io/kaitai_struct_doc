dist: bionic
sudo: false

branches:
  only:
    - master

language: ruby

addons:
  apt:
    packages:
      - graphviz
      - python-pygments

cache:
  - apt

before_install:
  - gem install asciidoctor pygments.rb

script:
  - pygmentize -V
  - make

after_success:
  - ./publish
