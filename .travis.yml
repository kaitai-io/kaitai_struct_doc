dist: xenial
sudo: false

branches:
  only:
    - master

language: ruby

addons:
  apt:
    packages:
      - graphviz
      - python3-pygments

cache:
  - apt

before_install:
  - gem install asciidoctor pygments.rb

script:
  - make

after_success:
  - ./publish
